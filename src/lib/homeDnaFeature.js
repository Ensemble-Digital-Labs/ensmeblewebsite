/**
 * Home page DNA helix WebGL layer.
 * - `ribbon` — fixed viewport ribbon (`DnaCapitalHelixCanvas`, same stack as `/experiments`)
 * - `document` — scroll-linked double helix (`HomePageDnaCanvas`, preserved for later)
 */
export const HOME_PAGE_DNA_HELIX_ENABLED = true
export const HOME_PAGE_HELIX_VARIANT = 'ribbon'
