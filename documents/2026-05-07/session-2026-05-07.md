# Session log - 2026-05-07

## Date & time
- 2026-05-07 22:53 (UTC-5)

## Summary
Added a reduced-motion fallback for scrolling so iPhone users can still scroll when smooth scrolling is disabled.

## Changes made
- Edited `src/app/layout.jsx`
- Created `documents/2026-05-07/session-2026-05-07.md`

## Decisions / notes
- When `prefers-reduced-motion` is enabled, `#main` now uses native vertical scrolling (`overflow-y-auto`) and no longer locks to `h-screen overflow-hidden`.
- Existing Locomotive/Lenis behavior is preserved for normal motion mode.

## Next steps
- Verify on iPhone with both Reduce Motion ON and OFF.

---

## Update (later same day)

## Summary
iPhone scrolling still failed when Reduce Motion was off because Lenis (Locomotive) was driving `#main` while it stayed `overflow-hidden`. Touch-primary devices now use native `#main` scroll with a ScrollTrigger `scrollerProxy`, and reduced-motion sections stay visible in CSS.

## Changes made
- Edited `src/lib/utils.js` — `shouldUseNativeMainScroll()`
- Edited `src/lib/locomotive.js` — native scroll branch + proxy; Locomotive only when not native
- Edited `src/app/layout.jsx` — `useNativeMainScroller` from touch + reduced-motion media queries; `native-main-scroll` class
- Edited `src/index.css` — `[data-scroll-section]` visible under reduced motion; `#main.native-main-scroll` iOS touch helpers

## Notes
- Desktop pointer + motion unchanged (Locomotive/Lenis).

---

## Update — iOS Reduce Motion off

## Summary
Scrolling worked with Reduce Motion on (native path) but not off: iOS Safari often does not match `(pointer: coarse)`, so Lenis stayed active. `shouldUseNativeMainScroll()` now also enables native `#main` scroll when `(hover: none)` and `navigator.maxTouchPoints > 0`.

## Changes
- Edited `src/lib/utils.js` — broadened touch detection
- Edited `src/app/layout.jsx` — media listeners for `(hover: none)` and `(pointer: coarse)` so layout stays in sync

---

## Update — iPhone scroll lag

## Summary
Reduced jank on touch / native `#main` scroll: hero particle field no longer runs a per-frame rAF + state loop on iPhone, 3D carousel updates only on scroll (not every frame), cinematic pins disabled on touch, scrub smoothing reduced, decorative CSS animations paused via `html.ensemble-touch-perf`.

## Changes
- `src/components/sections/HeroParticleField.jsx` — static field when `shouldUseNativeMainScroll()` (iOS often reports `pointer: fine`)
- `src/components/ui/CircularGallery.jsx` — scroll/resize-driven updates on touch; `getPinZoneProgress` supports Lenis or native main
- `src/lib/cinematicSectionReveal.js` — no pin + `scrub: true` on touch perf path
- `src/components/CinematicFooter.jsx` — `scrub: true` on touch perf path
- `src/app/layout.jsx` — toggles `ensemble-touch-perf` on `<html>`
- `src/index.css` — pauses heavy hero/carousel/footer keyframes under `.ensemble-touch-perf`
