# Session log – 2026-04-13

## Summary
Rebuilt the home hero to match the provided reference: dark canvas, plexus-style globe, two-line headline, gold primary CTA, scroll cue, plus desktop nav links and home-only cookie/chat affordances.

## Changes
- `src/canvas/HeroGlobePlexus.jsx` — new Three.js globe (points + k-nearest edges), reduced-motion radial fallback.
- `src/components/sections/Hero.jsx` — simplified layout; removed video/pain-points/trust strip from hero; GSAP entrance for headline + CTA.
- `src/lib/content.js` — hero copy (`Engineering` / `Digital Excellence`), `Explore Our Labs` → `/insights`, `backgroundVideo: null`, empty `painPoints`.
- `src/data/navigation.js` — `navBarLinks` (Work, About, Labs) + extended `navLinks` for fullscreen menu.
- `src/components/FullscreenNav.jsx` — desktop inline links; Contact CTA copy/styling; `nav-cta--contact-light` for CSS overrides.
- `src/index.css` — overrides for white text / violet border on contact CTA.
- `src/components/CookieConsent.jsx`, `src/components/ChatWidget.jsx` — home floating UI (cookie dismiss + contact shortcut).
- `src/pages/Home.jsx` — render cookie + chat when loader completes.

## Notes
- Privacy link in cookie banner points to `/contact` until a dedicated privacy route exists.
- Chat widget links to `/contact` (placeholder for future live chat).
- Optional follow-up: lazy-load `HeroGlobePlexus` to trim main bundle size.

---

## Update — remove desktop Work / About / Labs strip
- `src/components/FullscreenNav.jsx` — removed the `md:flex` inline nav links (Work, About, Labs); Contact + menu unchanged. Fullscreen menu still lists all `navLinks` from `navigation.js`.

---

## Update — remove cookie consent banner
- Removed `CookieConsent` from `src/pages/Home.jsx` and deleted `src/components/CookieConsent.jsx`.

---

## Update — hero globe: medical / digital marketing globe
- `src/canvas/HeroGlobePlexus.jsx` — inner dense land point-cloud (parametric continent blobs), larger outer plexus shell with nodes, teal/cyan core glow, subtle emerald DNA-style helix strands, EKG-like line pulse; slower drift; reduced-motion gradient updated for cyan/teal.

---

## Update — globe layering: Earth surface outside, network wraps outside
- `src/canvas/HeroGlobePlexus.jsx` — single surface radius `R_SURFACE` for all Earth dots; full-sphere faint “ocean” shell + brighter land dots; removed inner solid core; very subtle teal rim mesh on the outer shell; plexus + nodes at `~1.16×` radius; helix between surface and network; halo outside network.

---

## Update — detailed Earth land sampling
- `src/canvas/HeroGlobePlexus.jsx` — 2048×1024 equirectangular land mask built from many overlapping ellipse regions (continents, islands, Antarctica); land points sampled from mask with coastal bias; ~6200 land + ~7200 ocean dots; coarse blob fallback if sampling under-fills.

---

## Update — hero background match reference (black + cyan→violet globe + bloom)
- `src/components/sections/Hero.jsx` — `bg-black`, removed purple/blue CSS gradient overlay behind the globe.
- `src/canvas/HeroGlobePlexus.jsx` — black scene/clear; no fog; cyan→violet gradient on ocean, land, and plexus (by `nx`); removed DNA helix; teal rim + purple halo; `EffectComposer` + `UnrealBloomPass` for soft glow; renderer opaque black; reduced-motion fallback on black with cyan/violet radial.

---

## Update — sparse fragmented outer plexus (reference-style net)
- `src/canvas/HeroGlobePlexus.jsx` — outer shell ~108 nodes, only **2** nearest-neighbor links per node (was 400×4); edges culled with low-frequency **void** field, secondary **patch** noise, and deterministic **thinning** so large empty areas and gaps; plexus **nodes** only at endpoints of surviving edges; slightly softer line/node opacity.

---

## Update — net closer to reference img 1 (vs current img 2)
- `src/canvas/HeroGlobePlexus.jsx` — fewer shell nodes (**68**); **angular cap** on edges (`minDirDot` 0.86) so no long chords; stronger void/patch cull + new **rip** high-frequency holes; deterministic thin keeps **~22%** of edges; line colors nudged toward **white**; lower line/node opacity and size; pulse/bloom tuned for thinner lines (bloom slightly softer).

---

## Update — real Earth texture + interactive orbit
- `public/assets/images/earth-day-2048.jpg` — equirectangular Earth (from three.js examples / NASA-style asset) for accurate geography on a shaded sphere.
- `src/canvas/HeroGlobePlexus.jsx` — replaced point-cloud land/ocean with **MeshStandardMaterial** sphere + texture; **OrbitControls** (drag rotate, wheel zoom, damping); gentle **autoRotate**; lights (ambient + directional); outer plexus + halo retained; bloom tuned for photo Earth; container `cursor-grab` / `touch-none`; **reduced motion** still uses static gradient (no WebGL).
- `src/components/sections/Hero.jsx` — `pointer-events-none` on hero text stack, `pointer-events-auto` on CTA + scroll link so drags pass through to the globe.

---

## Update — spin the Earth (not the camera); dots on skin
- `src/canvas/HeroGlobePlexus.jsx` — removed **OrbitControls**; camera fixed at origin-facing setup. Wrapped textured Earth, **surface dot cloud** (sampled from the **same** equirect texture at `R_SURFACE`), outer plexus, and halo in **`earthSpinGroup`**; pointer + touch drag rotates that group only. Touch handlers refactored to **named functions** so `touchend` / `touchcancel` cleanup is correct.
- Dots are placed at slightly above sphere radius so they read as sitting on the globe skin with geography alignment.

---

## Update — tech polish + dot alignment with textured sphere
- `src/canvas/HeroGlobePlexus.jsx` — **UV sampling** now matches **THREE.SphereGeometry** (`u = atan2(z,-x)`, `v = 1 - acos(y)/π`) so canvas pixel reads line up with the same Earth texture on the mesh; land/ocean uses **blue-channel lean** + luminance (`isOceanPixel`) instead of luminance-only; **uniform sphere** random directions for dot placement; slightly tighter **skin** offset; higher-res readback cap (4096×2048).
- **Interaction**: **inertial coast** after pointer/touch release (damped angular velocity), subtle **scale-up** while dragging, brighter **network** opacity when dragging.
- **Look**: Earth **metalness/emissive** nudge, denser surface dots, thinner additive points, **cyan rim shell** + tweaked halo; rim pulses slightly with drag/time.

---

## Update — site-wide visual theme tokens (surfaces, type, patterns)

**Why:** Unify repeated hex grays and `#0891B2` across marketing sections so the brand reads as one system (cyan + gold palette, Matter + Antique Olive).

**Files**
- `src/lib/constants.js` — `background.subtle`, `background.muted` for `initTheme()` alignment with CSS.
- `src/index.css` — `--color-background-subtle`, `--color-background-muted`, `--color-border-muted`, `--color-pattern-dot`; `.bg-pattern-dots` utility; `.logo-txt` uses `var(--color-brand-primary)`.
- `tailwind.config.js` — `surface.*`, `line.muted`, `fontFamily.sans` → Matter stack, `fontFamily.display` → Antique Olive; `boxShadow` tokens `card`, `card-hover`, `section`.
- `src/components/ui/SectionHeading.jsx` — display font + tighter leading on section titles.
- `src/components/sections/CTABand.jsx`, `ServiceTiers.jsx`, `WhyChooseUs.jsx`, `AboutHero.jsx`, `src/pages/Contact.jsx` — swap hardcoded colors for theme classes (`bg-surface-subtle`, `text-brand-primary`, `text-text-primary` / `text-text-muted`, `border-line-muted`, etc.).

**Notes / next steps:** Other pages (Services grid, FAQ, Case Studies, carousel) still mix raw Tailwind grays in places; extend the same token pattern there when touching those files. Rainbow CTA borders remain as a deliberate accent; optional follow-up is a “brand-only” hover variant for stricter minimalism.

---

## Update — loading screen polish

**Why:** The previous loader faded out almost immediately and used a generic pill-shaped pulse; the logo did not get a clear “hold” before exit.

**Files**
- `src/components/Loader.jsx` — ~1.1s minimum hold before exit; layered radial gradients + vignette on `#070708`; gradient progress line (cyan → gold) driven by GSAP during hold; smoother exit (`power3`, slight upward move); `aria-busy` / progressbar semantics; reduced motion: shorter timings, no bar animation, full bar shown statically; timeline cleanup on unmount; removed old border/pulse control.

**Notes:** `npm run build` passes.

---

## Update — restore `COMPLETION_GUIDE.md`

**Why:** File had been removed from the working tree; Cursor rules still reference it.

**Files**
- `COMPLETION_GUIDE.md` — restored from git (`aa21b6e`) and rewritten for current repo: hero globe (not empty video), PageTransition/`locomotiveScroll` status, loader/theme-token progress, removed cookie banner, optional lazy-load globe, phased checklist unchanged in spirit.
- `.cursor/rules/completion-priorities.mdc` — synced bullets with the guide (removed stale PageTransition fix; added theme-token pass + lazy-load notes).

---

## Update — Manus / external builder handoff doc (motion & animation)

**Files**
- `documents/manus-ensemble-website-motion-handoff.md` — new: stack (GSAP, ScrollTrigger, Locomotive/Lenis, Three.js), `#main` scroller contract, `data-scroll-section`, Loader + route transitions, hero globe notes, reduced motion, file map, checklist for tools like Manus.

**Notes:** Safe to share with anyone building or exporting layouts so scroll-driven animation stays consistent.

---

## Update — animated Ensemble Digital Labs logo (branding assets)

**Summary**  
Implemented a reusable animated logo using `ensemble-logo.svg` / `ensemble-logo-light.png`, wired into nav, home loader, route transition overlay, and footer; favicon points at the SVG; menu-open state uses `data-menu-open` + CSS instead of per-letter color.

**Changes**
- `src/components/AnimatedBrandLogo.jsx` — new; variants `nav` | `loader` | `transition` | `footer`; optional `imgAlt` for accessible links.
- `src/index.css` — `brand-logo-ambient` / `brand-logo-footer-soft` keyframes; reduced-motion and `.reduced-motion` overrides; nav menu-open glow.
- `src/components/FullscreenNav.jsx` — image logo + `data-menu-open`; removed letter-span refs.
- `src/components/Loader.jsx`, `src/components/PageTransition.jsx` — logo image instead of “ENSEMBLE” text.
- `src/components/sections/Footer.jsx` — logo lockup + link home.
- `src/lib/branding.js` — `mark` URL temporarily uses primary SVG (no missing `ensemble-mark.png`).
- `index.html` — favicon → `/assets/branding/ensemble-logo.svg`.

**Notes**  
Animation is CSS-only (glow + float); users with `prefers-reduced-motion` or `.reduced-motion` get a static logo.

---

## Update — hero: remove video; globe + animated horizon background

**Summary**  
Replaced the hero MP4 with the existing Three.js Earth + plexus (`HeroGlobePlexus`), added dual cyan/violet HUD horizon rings in the scene, a screen-space gradient horizon line (CSS), vignette overlays for readability, and `pointer-events` so drag-to-spin still works while CTAs stay clickable.

**Changes**
- `src/lib/content.js` — `backgroundVideo: null`.
- `src/components/sections/Hero.jsx` — removed `<video>`; import/render `HeroGlobePlexus`; `min-h-[100svh]`, `bg-black`; vignette + `.hero-horizon` markup; `pointer-events-none` on copy, `pointer-events-auto` on CTAs.
- `src/canvas/HeroGlobePlexus.jsx` — fixed `horizonGroup` with two additive `RingGeometry` meshes, slow yaw + opacity pulse in `animate`; dispose on unmount.
- `src/index.css` — `hero-horizon-sweep`, `hero-horizon-glow-pulse`, reduced-motion overrides.

**Notes**  
Bundle size increases with Three on the main chunk; optional later: `React.lazy` for the globe.

---

## Update — hero globe idle rotation

- `src/canvas/HeroGlobePlexus.jsx` — added constant **idle yaw** (`IDLE_YAW_SPEED`, ~0.1 rad/s) when not dragging, frame-time `dt`-scaled, so Earth visibly rotates on load; drag + inertia unchanged.

---

## Update — cooler “net” horizon (CSS + 3D plexus)

- `src/canvas/HeroGlobePlexus.jsx` — replaced simple horizon rings with **`buildHorizonNetGeometry`**: 6 concentric segmented circles + spokes + crossing chords; additive `LineSegments`; outer **ring glow** mesh; faster yaw + richer opacity pulse.
- `src/components/sections/Hero.jsx` — taller horizon stack: **dual mesh layers**, **scan sweep**, **edge pulse line**, **radial glow**, gradient fade.
- `src/index.css` — `hero-net-drift` / `hero-net-drift-b`, scan + edge + glow keyframes; reduced-motion disables motion.

---

## Update — Page2 background blend from Hero

- `src/components/sections/Page2.jsx` — `bg-bg-primary`, `overflow-hidden`; top stack blends **black → deep blue-gray → transparent** (masked soft bottom); cyan/violet **screen** wash + **radial** + thin **horizon line** echoing hero HUD; content `relative z-10`.

---

## Update — Carousel3D (#page3) background

- `src/components/sections/Carousel3D.jsx` — explicit `bg-bg-primary`; layered **backdrop** (glow + diagonal mesh + grid + vignette + soft dark top fade from section above); carousel + controls `z-10`; `prefersReducedMotion` disables mesh drift.
- `src/index.css` — `.carousel-3d-bg-*` utilities + `carousel-bg-mesh-drift` keyframes.

---

## Update — medical HUD image as hero background

- Copied user asset to `public/assets/images/hero-medical-hud-bg.png`.
- `src/lib/content.js` — `heroContent.backgroundImage` points at that file.
- `src/components/sections/Hero.jsx` — full-bleed `<img>` under 3D layer + light teal/dark gradient wash for readability.
- `src/canvas/HeroGlobePlexus.jsx` — optional `blendWithBackdrop`: transparent WebGL clear + `scene.background = null` when compositing over the bitmap; reduced-motion path removed from component (Hero omits globe when reduced motion; static image remains).

---

## Update — Carousel3D (#page3) uses same HUD bitmap as hero

- `src/components/sections/Carousel3D.jsx` — imports `heroContent`; when `backgroundImage` is set, full-bleed `<img>` + gradient wash (`from-black/30` → `to-bg-primary`) + soft radial lift + low-opacity grid; if `backgroundImage` is null, restores prior mesh/glow/vignette stack.
