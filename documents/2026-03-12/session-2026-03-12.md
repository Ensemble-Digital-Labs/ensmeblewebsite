## Summary
Updated Contact page typography so key highlighted phrases use a distinct italic font treatment.

## Changes
- Tweaked `src/components/sections/ContactHero.jsx` to give the highlighted span a serif italic font with wider tracking.
 - Adjusted `src/pages/Contact.jsx` so the “next mission.” phrase in the form heading uses the same serif italic styling.
 - Updated `src/pages/Contact.jsx` so the contact form underline borders use the brand cyan `#0891B2` (subtle by default, stronger on focus) to align with the page theme.
 - Replaced the placeholder "Interactive Map Initialization" card in `src/pages/Contact.jsx` with a live embedded Google Map and matching gradient/dotted-card styling.
 - Pointed the embedded Google Map in `src/pages/Contact.jsx` to `11715 Administration Dr, St. Louis, MO 63146, USA`.
 - Reverted the Contact info panel styling in `src/pages/Contact.jsx` back to the original theme while updating only the physical address text to the St. Louis office.
 - Updated the fullscreen nav "Get in touch" address line in `src/components/FullscreenNav.jsx` to the same St. Louis address.

## Notes
- Only typography of the emphasized phrases changed; the text content and layout remain the same.

---

## Summary
Refreshed the Services grid cards with a more modern, lighter visual treatment.

## Changes
- Updated `src/components/sections/ServicesGrid.jsx` to remove fixed card heights and use a flexible min-height so shorter descriptions don’t leave large empty areas.
- Wrapped each service card in a gradient border shell with a darker glassmorphism interior for a more premium, modern look.
- Softened and brightened the background imagery so it’s more visible, and refined hover states (lift, shine sweep, and CTA reveal) for a smoother, high-end interaction.

## Notes
- Structural layout (3-column grid and routing to Contact) is unchanged; only visual card styling and spacing were adjusted.

---

## Summary
Polished the Services grid icon treatment and card borders for consistency.

## Changes
- Updated `src/components/sections/ServicesGrid.jsx` so the service icon now sits inside a circular gradient badge with an inner dark circle, giving it a cleaner, more modern feel.
- Adjusted the card body to use `h-full` along with `min-h` so all cards in a row have consistent height, preventing the thicker bottom-border effect on the 2nd and 3rd cards.

## Notes
- Only visual refinements were made; content and links remain unchanged.

---

## Summary
Swapped emoji service icons for vector icons from an installed library.

## Changes
- Installed `lucide-react` and imported a small set of icons (`Target`, `LineChart`, `Globe2`, `Palette`, `BarChart3`, `Rocket`) for the Services grid.
- Updated `src/components/sections/ServicesGrid.jsx` to map each service `id` to a corresponding Lucide icon, rendered inside the existing gradient circular badge.

## Notes
- The `services` data still includes emoji, but the grid now uses Lucide icons for a cleaner, more consistent look.

---

## Summary
Redesigned the FAQ section with a more modern, glassy accordion style.

## Changes
- Updated `src/components/sections/FAQ.jsx` to add ambient gradient background glows and convert each FAQ item into a glassmorphism card with subtle depth and hover/active shadows.
- Replaced the plain SVG chevron with the `ChevronDown` icon from `lucide-react`, wrapped in a circular control that rotates and highlights on open.
- Added numbered badges for each question, refined typography, and adjusted the expand/collapse animation for smoother transitions.

## Notes
- The FAQ content structure (questions/answers and data source) remains the same; only the presentation and interactions were modernized.

---

## Summary
Softened FAQ colours to a more neutral, modern glass look.

## Changes
- Updated `src/components/sections/FAQ.jsx` to use a deeper neutral background with subtle white and brand glows instead of strong cyan/brand blobs.
- Adjusted FAQ cards to use near-black glass (`bg-white/[0.02]`) with white-focused borders/accents, plus more neutral numbered badges and chevron button states.

## Notes
- Colour palette is now mostly neutral with restrained brand usage, to better match a contemporary premium SaaS/agency aesthetic.

---

## Summary
Improved FAQ heading and badge visibility on the dark background.

## Changes
- Extended `SectionHeading` to support an `invert` mode that uses white typography instead of `text-text-primary`/secondary tokens.
- Updated the FAQ section to use the inverted heading style and brightened the numbered badges (stronger white text and subtle background) so they are clearly visible against the new dark backdrop.

## Notes
- Other sections keep their existing SectionHeading colours; only FAQ opts into the inverted style.

---

## Summary
Increased contrast of FAQ question rows against the dark background.

## Changes
- Strengthened the FAQ card background and border (`bg-white/[0.03]` with a slightly heavier white border) so the first question row clearly pops off the page.
- Added a visible focus ring to the FAQ button and ensured the question text is full white for maximum readability.

## Notes
- Layout and interactions are unchanged; this was purely a contrast/visibility tweak.

---

## Summary
Brightened FAQ answer text for better readability.

## Changes
- Updated the FAQ answer paragraph in `src/components/sections/FAQ.jsx` to use a lighter gray (`text-gray-300`) on the dark background.

## Notes
- Question titles remain full white; answers now sit one step down in the hierarchy but are clearly legible.



