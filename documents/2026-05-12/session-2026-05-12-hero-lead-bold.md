# Session log – 2026-05-12 (hero “Not just a” bold)

## Summary
Made the first hero headline line (`leadText`, e.g. “Not just a”) **bold** on mobile/tablet and desktop shutter rows by changing `font-medium` → `font-bold` on the wrapping elements in `HeroScrollExpand`.

## Changes
- `src/components/sections/HeroScrollExpand.jsx` — lead line typography (`< lg` stack + `lg+` top `ShutterWord` row).

## Next steps
- None.
