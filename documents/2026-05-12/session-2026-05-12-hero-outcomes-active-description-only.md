# Session log – 2026-05-12

## Summary
`HeroStatsTrustBand` feature “tabs”: body copy is visible only for the active item; inactive items show icon + title only. Description stays in the DOM with `sr-only` when inactive. Progress strip renders only when active.

## Changes
- `src/components/sections/HeroStatsTrustBand.jsx`
