# Session log – 2026-05-12

## Summary
Removed glass pill wrappers from the three marble-bar trust lines only; trust copy is plain `growth-gradient-text` again. Stat metric captions still use glass pills.

## Changes
- `src/components/sections/HeroStatsTrustBand.jsx` — trust column: dropped outer glass `div`, restored single `<p>` per line and original `gap-1` / `sm:gap-2`.
