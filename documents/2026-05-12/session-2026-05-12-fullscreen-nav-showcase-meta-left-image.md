# Session log – 2026-05-12 (fullscreen nav — showcase meta left, smaller image)

## Summary
Selected-work cards: **title unchanged** at top; **category / divider / client** sit in a **left column** beside a **smaller fixed thumbnail** (`h-28 w-36`, `xl:h-32 w-40`). Removed the old single-line meta under the title. Slightly reduced snap **`min-h`** on cards since the image block is shorter.

## Changes
- Edited `src/components/FullscreenNav.jsx`.
