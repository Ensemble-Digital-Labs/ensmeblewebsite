# Session log – 2026-05-12 (hero phase-2 aside larger + frame)

## Summary
Right column phase-2 copy is larger (`text-base` → `lg`/`xl` steps up to ~`text-xl`) with relaxed line-height. Replaced thin L-bracket ticks with a glass panel: rounded shell, border, blur, inset highlight, gradient top hairline, vertical gold accent rail, and softer corner marks.

## Changes
- Edited `src/components/sections/HeroScrollExpand.jsx` — `HeroScrollExpandPhase2Aside`.

## Notes
- Slightly wider max width on `lg`/`xl` for the longer line measure.

## Next steps
- None.
