# Session log – 2026-05-12 (hero secondary CTA matches primary)

## Summary
"See what we build" used the ghost `growthSecondaryHero` style. It now uses the same gradient pill + label/arrow layout as "Get your free practice audit" on `HeroScrollExpand` (mobile + desktop phase-2) and legacy `Hero`.

## Changes
- Edited `src/components/sections/HeroScrollExpand.jsx`
- Edited `src/components/sections/Hero.jsx`

## Notes
- Both CTAs share `growthPrimaryHero`; copy and `href` still distinguish them for users and screen readers.

## Next steps
- None.
