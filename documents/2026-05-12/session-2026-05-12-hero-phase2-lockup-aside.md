# Session log – 2026-05-12 (hero phase-2 left/right copy & weight)

## Summary
Desktop hero phase-2 left lockup now uses the “Not just a / marketing / agency” three-line story (aligned with `headlineLines`). The right aside (subhead paragraph) uses `font-bold` and `font-display` for stronger emphasis.

## Changes
- Edited `src/lib/content.js` — `heroScrollExpandPhase2Lockup` lines + comment.
- Edited `src/components/sections/HeroScrollExpand.jsx` — phase-2 aside typography.

## Notes
- Right column body still comes from `heroScrollExpandPhase2Aside` (joined `heroSubheadSegments`).

## Next steps
- None.
