# Session log – 2026-05-12

## Summary
Parallax showcase main `h2` now combines `section-heading-neon` (display weight / rhythm) with `growth-gradient-text` so the fill matches the primary CTA’s **growth-from → growth-to** gradient instead of the softer multi-stop `section-heading-neon` fill alone.

## Changes
- Edited `src/components/sections/ParallaxLayerShowcase.jsx`

## Notes
- Build: `npm run build` OK.
