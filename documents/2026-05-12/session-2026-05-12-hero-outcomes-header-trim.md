# Session log – 2026-05-12

## Summary
Removed the practice outcomes eyebrow (“Practice outcomes”) and kicker (“Ensemble Digital Labs”) lines from `HeroStatsTrustBand` so the section opens on the main headline and CTA only.

## Changes
- Edited `src/components/sections/HeroStatsTrustBand.jsx`

## Notes
- `heroPracticeOutcomeSection.eyebrow` / `.kicker` remain in `content.js` for reuse or future UI; only the band header no longer renders them.
