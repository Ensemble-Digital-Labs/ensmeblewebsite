# Session log – 2026-05-12

## Summary
Rendered marble-bar trust lines (`trustLabel` parts) in all caps via `uppercase` and tuned letter-spacing for readability.

## Changes
- `src/components/sections/HeroStatsTrustBand.jsx` — trust column `<p>`: `uppercase`, positive `tracking-*` instead of tight negative tracking.
