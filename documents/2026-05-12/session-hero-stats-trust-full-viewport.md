# Session log – 2026-05-12 (hero stats band full-viewport height)

## Summary
`HeroStatsTrustBand` is now a full-viewport band: `min-h-[100svh]` with `flex flex-col justify-center` so the outcomes + trust card sits vertically centered in at least one screen height; slightly increased vertical padding.

## Changes
- Edited `src/components/sections/HeroStatsTrustBand.jsx`

## Notes
- Inner `Container` / glass card max width unchanged for readable measure on wide screens.

## Next steps
- None.
