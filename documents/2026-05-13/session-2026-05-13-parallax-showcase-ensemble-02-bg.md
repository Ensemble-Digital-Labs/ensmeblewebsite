# Session log – 2026-05-13 (Parallax showcase ensemble-02 backdrop)

## Summary
Layered `hero-bg-ensemble-02.png` (`ambientAssets.ensemble02`) behind `#parallax-showcase` as a full-bleed `object-cover` image with a navy gradient scrim so headline, lead, and glass pillar cards stay readable.

## Changes
- Edited `src/components/sections/ParallaxLayerShowcase.jsx` — import `ambientAssets`, decorative `img` + overlay, JSDoc note.

## Next steps
- Tune `opacity` / gradient stops if art direction wants more or less of the PNG visible.
