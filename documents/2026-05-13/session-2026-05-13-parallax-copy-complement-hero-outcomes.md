# Session log – 2026-05-13 (Parallax vs hero outcomes copy)

## Summary
Addressed overlap between `HeroStatsTrustBand` (AI-first, HIPAA-safe, revenue) and `ParallaxLayerShowcase` pillars. Rewrote `parallaxShowcaseContent` lead and four pillars to emphasize operating model, nuance, and reporting—without repeating the hero outcomes rail. Updated `ParallaxLayerShowcase.jsx` JSDoc to note the complementary intent.

## Changes
- Edited `src/lib/content.js` — `parallaxShowcaseContent` lead + `pillars` labels/body copy.
- Edited `src/components/sections/ParallaxLayerShowcase.jsx` — component JSDoc only.

## Notes
- Lead avoids UI-relative phrasing (“section above”) so reordering Home bands does not break meaning.

## Next steps
- Optional: tighten headline (“Built like this”) if product wants an even clearer bridge from outcomes to process.
