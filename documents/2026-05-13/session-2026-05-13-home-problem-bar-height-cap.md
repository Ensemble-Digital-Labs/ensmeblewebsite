# Session log – 2026-05-13

## Summary
Capped home-problem **image bar height** with a dedicated clip layer: responsive `h-[clamp(...)]` (~136–192px band) + `object-cover` + `object-[center_40%]` so strips stay shorter; trades a little vertical crop for consistent bar size.

## Changes
- Edited `src/components/sections/HomeProblemSection.jsx`
