# Session log – 2026-05-13

## Summary
Home problem section heading is two stacked lines with uppercase styling; content uses `headlineLine1` / `headlineLine2` (neon + line2 gradient classes, aligned with `Carousel3D`).

## Changes
- Edited `src/lib/content.js` — `homeProblemContent`: `headline` → `headlineLine1` + `headlineLine2`
- Edited `src/components/sections/HomeProblemSection.jsx` — single `h2` with two spans, `uppercase` + tracking on wrapper

## Next steps
- None
