# Session log – 2026-05-13

## Summary
Added optional **`imageObjectPosition`** on home-problem pains; **Wasted ad spend** uses `center 72%` so `object-cover` crops favor the **lower** part of the banner. Component applies `style.objectPosition` (default `center center`).

## Changes
- Edited `src/lib/content.js` — `pains` doc + wasted ad `imageObjectPosition`
- Edited `src/components/sections/HomeProblemSection.jsx` — img `objectPosition` from content
