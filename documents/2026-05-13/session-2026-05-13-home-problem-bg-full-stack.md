# Session log – 2026-05-13

## Summary

Kept all pain rows **inside** the same ambient band as the headline (structure was already correct) and made the wash read through the **full stack**—including **Fragmented vendors**—by **softening the bottom scrim** (`to` / `via` opacities) and adding **bottom padding** on that band so the image + gradient extend visibly below the last card.

## Changes

- Edited `src/components/sections/HomeProblemSection.jsx` — problem-band wrapper: `pb-8 sm:pb-10 md:pb-12`; gradient `from-[#050816]/72 via-[#050816]/78 to-[#050816]/84` (was heavier toward `/94`).

## Notes

- If the headline needs more contrast again, nudge only `from-*` slightly darker while keeping `to-*` lighter.
