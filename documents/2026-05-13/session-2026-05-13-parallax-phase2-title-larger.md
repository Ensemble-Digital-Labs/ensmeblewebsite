# Session log – 2026-05-13 (Parallax phase-2 title larger)

## Summary
Increased Phase 2 (revealed) pillar title typography: `font-display` + responsive `clamp` sizes up to ~`1.35rem` on `md`, `font-bold`, wider `max-w` for glass headers and split static headers. Raised `ParallaxPillarDescription` top padding so body copy clears the taller title band.

## Changes
- Edited `src/components/sections/ParallaxLayerShowcase.jsx`.

## Next steps
- None.
