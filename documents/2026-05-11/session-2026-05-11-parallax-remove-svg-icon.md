# Session log – 2026-05-11

## Summary
Removed the decorative Osmo-style SVG above the pillar grid in `ParallaxLayerShowcase`.

## Changes
- `src/components/sections/ParallaxLayerShowcase.jsx` — deleted the centered icon wrapper; pillar `<ul>` no longer uses `mt-2` (icon spacing).
