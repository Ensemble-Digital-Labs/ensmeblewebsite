# Session log – 2026-05-11

## Summary
Persisted recent Home/visual conventions into **always-applied** Cursor project rules so future sessions inherit the same decisions.

## Changes
- `.cursor/rules/project-context.mdc` — new **“Conventions (persistent)”** block: flat `#050816` canvas, no decorative section hairlines, `Carousel3D` flat default, testimonial/share `ParallaxDepth` `layer1` override, typography (Fraunces + Jakarta, no Runtime).

## Notes
- Tweak or remove this block if brand direction shifts back to layered atmospheres site-wide.
