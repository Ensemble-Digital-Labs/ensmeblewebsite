# Session log – 2026-06-15 (contact hover sequential type)

## Summary
Unified “Get in touch” as one Fraunces gradient line; words animate in/out sequentially via GSAP stagger (not instant pop).

## Changes
- `src/components/contact-orb/ContactOrbCursorMorph.jsx` — word-by-word GSAP reveal + reverse exit
- `src/styles/contact-orb-cursor-morph.css` — single line lockup, overflow masks per word
