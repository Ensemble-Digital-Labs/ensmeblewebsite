# Session log – 2026-04-27 (push to Ensemble org repo)

## Summary
Committed pending workspace changes and pushed `main` to `https://github.com/Ensemble-Digital-Labs/ensmeblewebsite.git` via a new Git remote named `ensemble`.

## Git
- Commit: `feat: sync site work — particle hero, cinematic sections, testimonials assets, footer and Page4 polish`
- Remote added: `ensemble` → org URL above (`origin` still points at `giachinh1/ensemblev2`)
- Branch: `main` pushed and set to track `ensemble/main`

## Notes
- To make the org repo the default push target: `git push` (already tracks `ensemble/main`) or run `git remote rename origin personal` and `git remote rename ensemble origin` if you want a single `origin`.
